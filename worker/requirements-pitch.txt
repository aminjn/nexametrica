# NOTE: the Z440 worker does NOT need anything here. Auto per-frame calibration
# runs on the LOCAL .pt via ultralytics (already in requirements.txt) — fully
# offline, no Roboflow, no key.
#
# This file is only for get_pitch_model.py --roboflow, which you run ONCE on a
# machine that has internet to fetch the model weights. Skip it entirely if you
# obtain the .pt another way (--url / --file).
inference>=0.21.0
