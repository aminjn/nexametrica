# Automatic per-frame pitch calibration (optional). Installed best-effort by
# setup-worker.sh; if it fails (deps/network), the worker still runs with the
# manual 4-corner calibration. Set PITCH=0 in .env to disable entirely.
#
# `inference` pulls a newer supervision than the core pin — that is expected;
# install it in the SAME venv last so its resolution wins for the pitch model.
inference>=0.21.0
sports @ git+https://github.com/roboflow/sports.git
